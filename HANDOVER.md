# The Everything Code — Session Handover

Read this first, then `DEV_NOTES.md` for the deep architecture detail. This is the "how to work
on this safely" guide plus the current state and next-task brief. Last updated end of the session
that built **The Business Cycle tab** and the **GMI Financial Conditions Index (FCI) reconstruction**.

## Live state
- Repo (public): https://github.com/Islandboy1968/global-m2  (slug is still `global-m2`)
- Live site: https://islandboy1968.github.io/global-m2/  (GitHub Pages, root of `main`)
- Latest `main` at handover: `2437dae` (FCI lead set to 12 months). Data refreshes daily.
- Product name: **The Everything Code (TEC)**. Four tabs now: Global Liquidity, US Liquidity,
  The Big Picture, **The Business Cycle**.
- **PAT expires 2026-06-20** — ask Raoul for a fresh fine-grained token after that. Never commit it.

## Environment gotchas (READ — these will bite a fresh session)
1. **Clone into a dir YOUR sandbox user owns.** A stale `/tmp/work/global-m2` may exist owned by
   `nobody` and be unwritable. Clone fresh to `~/gm` (i.e. `/sessions/<id>/gm`) and
   `git config --global --add safe.directory ~/gm`. The Read/Edit tools CANNOT see `~/gm` or `/tmp`
   — they only reach the mounted outputs dir. Workflow: edit a file in the mounted outputs dir with
   Write/Edit, then `cp` it into `~/gm` for git; read repo files with `cat` in bash.
2. **TradingView IS reachable from the sandbox** (websocket via `tv_pull.pull_series`), so you can
   pull market data and fit/iterate locally — this is how the whole FCI was built. **FRED is NOT
   reachable** from the sandbox (times out); FRED-sourced series (US/Big Picture) can only be
   regenerated by the GitHub Action runner. `web_fetch` is provenance-locked.
3. **`/tmp` can be wiped between bash calls.** Re-fetch defensively. Do file-write + `git add` +
   commit + push in the same bash call when using heredocs.
4. **Pushing** uses an inline token URL, redact it in output:
   `git push "https://x-access-token:<TOKEN>@github.com/Islandboy1968/global-m2.git" HEAD:main`
   then pipe through `sed -E 's/github_pat_[A-Za-z0-9_]+/[REDACTED]/g'`.
5. **Frontend verify is headless** (no browser): strip `<script>` blocks, run the last app script in
   jsdom with a fake `Chart` class that records `data`/`options`/`scales`, bind id'd elements onto
   `window`, eval the app, assert on `window.GLOBAL/US/BIG/CYCLE`. Used all session; reliable.
6. **Trigger + poll the Action** (poll loops time out at ~45s, so poll with single sleeps):
   ```
   curl -fsS -X POST -H "Authorization: Bearer <TOKEN>" -H "Accept: application/vnd.github+json" \
     https://api.github.com/repos/Islandboy1968/global-m2/actions/workflows/main.yml/dispatches -d '{"ref":"main"}'
   # then GET .../actions/runs/<id> until completed/success (~4-6 min). git fetch + rebase after
   # (the Action commits a "data: refresh" on top of your push).
   ```

## What's built this session (on top of the prior liquidity/Big-Picture work)
- **The Big Picture, chart 05:** LFPR (black, LHS, with its own +5-month forward lead) vs the US
  5-year Treasury yield (`DGS5`, pink RHS). Because the LEFT series leads here, it uses
  `controlsFwdLeft`/`buildLagFwdLeft` (shift datasets[0], not [1]).
- **The Business Cycle tab** (`tab-cycle`, IIFE `CYCLE`, `window.CYCLE`): five blocks —
  01 **The Everything Code Dominoes** (fixed inline-SVG reference slide, no chart);
  02 **ISM vs Global Total Liquidity** (ISM black L, liquidity YoY pink R, adjustable lead def 6mo);
  03 **ISM vs ISM New Orders** (shared axis, lead def 1mo);
  04 **ISM vs GMI FCI** (ISM black + FCI blend blue + FCI ex-oil light-blue, all on left axis; ONE
     lead box shifts both FCI lines, default **12mo / 365d**; the blend-vs-ex-oil gap = oil risk);
  05 **The dominoes** (ISM black L + liquidity YoY pink R +6mo + FCI blend blue L +12mo, fixed leads).
- **`build_fci.py`** — the FCI reconstruction (see methodology below). `build_fci_set()` pulls inputs
  once and returns both `fci` (50% oil blend) and `fci_exoil`; wired into `update_data.py` as
  `cycle.fci` / `cycle.fci_exoil`.
- **`build_cycle.py`** — ISM PMI (`ECONOMICS:USBCOI`) + New Orders (`ECONOMICS:USMNO`) from
  TradingView (FRED dropped ISM on copyright). `cycle.ism` / `cycle.neworders`.
- **Revert to defaults** button (next to Copy layout): clears this device's `tec_charts` and reloads
  to the published baseline.
- **Published layout** (`data/layout.js`, `TEC_DEFAULT_LOCKS`) currently holds the 17 charts that
  existed before the 4 new cycle/FCI charts. **The new charts (cycIsmLiqC is in; cycIsmNoC, cycFciC,
  cycDomC, bigLfprY5C may not be) open at their default windows for everyone.** To pin them: lock
  them, hit Copy layout, paste the string back, commit to `data/layout.js`.

## GMI FCI reconstruction — methodology (so it's reproducible)
Goal: reconstruct GMI's Financial Conditions Index, which leads ISM ~9–12 months. GMI's own Jan-2026
Monthly: "a regression with an average nine-month lead… optimisers suggest a roughly twelve-month
lead." We use **12mo** as the dashboard default.
- Inputs (TradingView, monthly): 5-year yield `TVC:US05Y` (YoY *difference*, pts), dollar `TVC:DXY`
  (YoY %), oil `TVC:USOIL` (YoY %). Each z-scored.
- Composite (looseness) = **−( z(Δ5y) + z(Δdollar) + w·z(Δoil) ) / (2+w)**, i.e. rising rates/dollar/oil
  all read as TIGHTER → index falls → ISM falls ~12mo later. `w` = oil weight: **0.5 = headline blend**,
  **0 = ex-oil**. 3-month smoothed, then rescaled to ISM's mean/std so it overlays ISM in PMI units.
- **Copper was tested and dropped** — it's coincident (peaks at 0 lead), so it confirms ISM, it doesn't
  lead it. Oil signed as a *tightening* input (rising oil = worse conditions ahead) is what produces
  the long, strong lead.

## Key analytical findings this session (the macro context)
- **The domino chain (GMI canonical):** Financial Conditions → Liquidity → ISM → Assets, each with a
  lag. Assets are the LAST domino, not simultaneous with liquidity. The FCI is the earliest/faintest.
- **GLI vs ISM lead ≈ 6–7mo; FCI vs ISM lead ≈ 10–12mo** (recent-window cross-correlation). So GLI lags
  FCI by ~3mo and leads ISM by ~6–7mo. Validates the 6mo liquidity / 12mo FCI leads on the dashboard.
- **The 2022-23 FCI overshoot (fell to ~38 while ISM only hit ~46) was a RATES + DOLLAR shock, not
  oil.** Decomposition at the trough: ~−8 rates, ~−6 dollar, <−1 oil; blend and ex-oil bottomed
  identically. The gap to ISM = the capex/stimulus/liquidity cushion absorbing the monetary shock.
- **Ex-oil is the marginally better level-fit recently** (RMSE 4.06 vs 4.38) and the more honest base
  case; the blend's only extra weakness now is the recent oil tick → oil is the swing factor to watch.
- **Liquidity ROC (YoY) is comp-distorted — read the OUTRIGHT LEVEL instead.** Level at all-time highs
  (~136T) but last 3 months nearly flat (+0.35T/mo vs +1T/mo trailing). YoY peaked Jan-2026 (+12.4%),
  now ~8%; this is HARD comps (lapping late-24/early-25). EASY comps (mid-25 plateau) arrive summer/
  autumn 2026 → YoY firms back toward ~9% on continued rises; then HARD comps in Q1-2027 (lapping the
  Dec25-Jan26 surge) collapse it toward 1–3% mechanically. So don't read YoY as a peak signal.
- **GMI's own current house view (Jan/Feb 2026 Monthlies) aligns with "pause not peak":** the liquidity
  short-term top is mechanical (US gov shutdown + TGA rebuild draining liquidity); FC keeps rising at
  the 9–12mo lead "paving the way"; the ISM "went up… with more to come"; and the Jan "Bitcoin: The
  Everything Disconnect" piece argues assets are trading BELOW what liquidity/FCI/regression imply, so
  the setup is catch-up, not a top. Watch-item: if the liquidity level keeps stalling after the
  shutdown/TGA effects clear, AND oil sustains, the pause reclassifies as a peak.

## The GMI brain (knowledge base) — how to use it
MCP "uc" tools (Universal Code corpus, ~13.5k records incl. GMI Monthlies as `gmi_reference`):
`ask_uc` (grounded Q&A), `search_uc` (BM25 + filters), `brief_uc` (structured briefing),
`source_context_uc` (expand a record), `list_recent_uc`, `status_uc`. Mention "GMI" or set
`source_type="gmi_reference"` to include the Monthlies. Results are extractive/truncated; use
`quote_candidate` level for fuller quotable lines. The Jan-2026 and Feb-2026 Monthlies are the most
relevant docs for the FCI/liquidity/ISM debate.

## NEXT TASK (where we paused — Raoul's "best path forward")
Raoul concluded the YoY liquidity read is too base-effect-distorted and we should track liquidity
**outright**. Agreed next builds:
1. **Liquidity level + momentum panel** — the LEVEL plus a clean short-window momentum (3m/6m
   annualised change of the level), and the YoY shown only with its forward comp path flagged so it's
   never misread as a turn. Make this the PRIMARY liquidity read instead of YoY. (This is the one to
   build first.)
2. **Normalised dominoes panel** — ISM, Global Liquidity and FCI all z-scored on ONE standard-deviation
   axis with adjustable leads (default GLI +6mo, FCI +12mo), so the chain collapses onto one signal and
   the leads can be slid live. Default the FCI line to **ex-oil** with the blend as the oil-risk overlay.
3. Optional follow-ups: FCI component-decomposition chart (rates/dollar/oil contributions);
   demand-conditioned oil weight; broad TWI dollar (FRED) + weekly data for the FCI; lock the new
   charts into the published `data/layout.js`.

## Quick reference — recent commits
- `2437dae` FCI lead → 12mo · `f2304a5` GMI FCI reconstruction + 2 FCI charts · `ef97eab` Big Picture
  chart 05 (LFPR vs 5y) · `2f678a6` publish 17-chart layout + Revert button · earlier: Business Cycle
  tab + Dominoes, per-chart lock, Copy layout, fonts, Claude Design restyle.
